{
  "name": "project_grafkom",
  "description": "",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "deploy": "vite build && git add dist -f && git commit -m 'deploy' && git push origin `git subtree split --prefix dist main`:gh-pages --force"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@tweenjs/tween.js": "^23.1.2",
    "three": "^0.152.2",
    "tween": "^0.9.0"
  },
  "devDependencies": {
    "vite": "^4.3.8"
  }
}